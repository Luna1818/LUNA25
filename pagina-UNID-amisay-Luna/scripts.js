// Obtenemos el elemento del encabezado por su ID
const encabezado = document.getElementById('inicio');

// Agregamos un evento de clic al encabezado
encabezado.addEventListener('click', function() {
    alert('Bienvenidos a la UNID');
});
